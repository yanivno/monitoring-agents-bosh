set -e

# unpack telegraf-1.8.2_linux_amd64.tar.gz

tar xzf telegraf/telegraf-1.8.2_linux_amd64.tar.gz

pushd telegraf
	# copy telegraf/telegraf to packages
	mkdir -p "${BOSH_INSTALL_TARGET}/bin/"
	cp telegraf "${BOSH_INSTALL_TARGET}/bin/"
popd
