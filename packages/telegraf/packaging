set -e

# unpack telegraf-1.8.1_linux_amd64.tar.gz

tar xzf telegraf/telegraf-1.8.1_linux_amd64.tar.gz

pushd telegraf
	# copy telegraf/usr/bin/telegraf to packages
	cp usr/bin/telegraf "${BOSH_INSTALL_TARGET}/bin"
popd
